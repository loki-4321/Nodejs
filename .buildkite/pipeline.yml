agents:
  queue: "${BUILDKITE_AGENT_META_DATA_QUEUE:-default}"
steps:
  - label: 📦 Build & Package
    plugins:
      - seek-jobs/gantry#<gantry_version>:
          command: build
          file: containerImage.yaml
          region: us-east-1
  - wait
  - label: 🚀 Deploy
    plugins:
      - seek-jobs/gantry#<gantry_version>:
          command: apply
          environment: development
          file: service.yaml
          region: us-east-1
